version: "3.9"

services:
  paper2poster:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: bridge
    container_name: Paper2Poster-container
    ports:
      - "11320:8501"
    restart: unless-stopped
